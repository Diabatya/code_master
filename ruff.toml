line-length = 120
extend-exclude = [
    "firmware/libs/**",
    "assets/version_info.py",
]

[lint]
select = ["E9", "F", "E", "W", "B", "SIM", "UP"]

[lint.per-file-ignores]
# Словарь переводов — таблица данных «ключ: значение»; дробить строки
# ради лимита длины только ухудшит читаемость и diff-ы.
"models/translations.py" = ["E501"]
