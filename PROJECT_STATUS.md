# Текущее состояние проекта "Код Мастер"

## Реализованные функции

### 1. Главное окно и навигация
- ✅ Главное окно 800×600 с минимальным размером 640×480 и возможностью разворачивания.
- ✅ Верхняя панель 48px (#252538): логотип «Код Мастер», индикатор COM-порта, кнопки «Тема», «Язык», «Логи», «DBC», «Выход».
- ✅ Центральная область с двумя карточками «Обновить» и «Настроить» (200×80px, 14pt).
- ✅ «Обновить» открывает страницу прошивки STM32 с кнопкой «Назад».
- ✅ «Настроить» открывает отдельное окно настроек CAN.
- ✅ Если COM-порт не выбран, первое нажатие открывает модальный диалог выбора порта.

### 2. Триггеры
- ✅ 10 независимых блоков триггеров.
- ✅ Поля приёма: ID (HEX, 80px) + D0–D7 (HEX, 30px).
- ✅ Чекбокс «Активен», счётчик срабатываний, кнопка «Тест».
- ✅ Поля отправки: ID ответа + D0–D7 ответа.
- ✅ Задержка (мс) и выпадающий список действий: одиночная отправка, запуск/остановка циклической отправки.
- ✅ Сохранение/загрузка триггеров в JSON.
- ✅ Автозаполнение ID и данных при выборе сигнала из загруженного DBC.

### 3. Мониторинг CAN
- ✅ Две панели CAN1/CAN2 через `QSplitter`.
- ✅ Таблицы с колонками: Время, ID, D0–D7, DLC, Период, Счётчик, ASCII, Сигналы.
- ✅ Режим «Уникальные ID» по умолчанию и переключатель в «Поток».
- ✅ Подсветка изменённых байтов (#4A6A8A) на 2 секунды.
- ✅ Контекстное меню: копирование, создание триггера, «Показать варианты данных», «Битовая карта» 8×8.
- ✅ Фильтры: диапазон ID, список исключений, «Только известные ID».
- ✅ Экспорт: текущая таблица в CSV, поток в CSV, трассировка CarBus.
- ✅ Панель отправки с историей, циклической отправкой и кнопкой «Из DBC».

### 4. Интеграция DBC и сигналов
- ✅ `core/dbc_parser.py` использует `cantools` для загрузки DBC и декодирования физических значений (factor, offset, единицы).
- ✅ `DBCManager` хранит загруженную базу и путь к ней в `Config`.
- ✅ Колонка «Сигналы» в мониторинге показывает до 3 декодированных значений.
- ✅ В триггерах и панели отправки можно выбрать сигнал из DBC для автозаполнения ID и данных.
- ✅ Загрузка DBC доступна из главного окна и из мониторинга.
- ✅ В гибкой логике реализован выбор сигнала DBC для автозаполнения условий правил.

### 5. Графики сигналов
- ✅ Новая вкладка «Графики» на `pyqtgraph`.
- ✅ Выпадающие списки «Узел» → «Сообщение» → «Сигнал» из DBC.
- ✅ Кнопка «Добавить график» и область с легендой, сеткой, масштабированием, паузой.
- ✅ При получении кадра соответствующего сообщения добавляется точка (время – физическое значение).
- ✅ Поддержка нескольких сигналов на одном графике и скользящее окно 60 секунд.

### 6. CAN-шлюз
- ✅ Полноценная вкладка «Шлюз» в окне настроек.
- ✅ Секция «Игнорировать» с 10 ID HEX, активируемая чекбоксом.
- ✅ 10 правил подмены: ID и данные приёма, ID и данные подмены, направление (CAN1↔CAN2), чекбокс «Активен».
- ✅ Прозрачная ретрансляция между каналами, если правило не сработало.
- ✅ Сохранение и загрузка правил в JSON.

### 7. Гибкая логика
- ✅ Редактор правил if-then для CAN-кадров.
- ✅ Кнопка «Из DBC» в диалоге правила для автозаполнения ID и эталонных данных по выбранному сигналу.
- ✅ Автоматическое заполнение маски данных (FF для всех байт) при выборе сигнала.
- ✅ Обработка правил в реальном времени с задержкой и заменой по маске.

### 8. Прошивка STM32
- ✅ Страница прошивки в главном меню, кнопка «Назад».
- ✅ Выбор .bin/.hex, индикация прогресса, фоновый поток.
- ✅ Интеграция с `core/bootloader.py` и AN3155.

### 9. Кроссплатформенность и сборка
- ✅ Автоопределение имён портов через `serial.tools.list_ports` (Windows/macOS/Linux).
- ✅ Горячие клавиши: `Cmd` на macOS, `Ctrl` на других платформах.
- ✅ Открытие папки логов через `subprocess.run` (open/xdg-open/os.startfile).
- ✅ `build_win.spec`, `build_mac.spec`, `build_linux.spec` проверены: в `hiddenimports` перечислены `PySide6`, `matplotlib`, `pyqtgraph`, `cantools`, `serial`, `logging.handlers` и все внутренние модули.
- ✅ `build_mac.spec` корректно формирует `.app` через `BUNDLE`.
- ✅ `.github/workflows/build.yml` с тремя job'ами (Windows, macOS, Linux): сборка `.exe`, `.dmg` через `hdiutil` и исполняемого файла Linux; единый релиз по тегу `v*` и автоматический `latest` pre-release при каждом пуше в `main`.

## Общая оценка готовности

**95 %**

Все запланированные блоки реализованы: главное окно, триггеры, мониторинг, DBC-интеграция, графики сигналов, CAN-шлюз, гибкая логика с DBC, прошивка STM32 и кроссплатформенная сборка. Приложение запускается и компилируется. Оставшиеся 5 % — это ручное тестирование на реальном CAN-оборудовании на всех трёх платформах.

## Примечание

Проект готов к выпуску первой стабильной версии. Рекомендуется провести тестовую сборку и ручное тестирование на целевых ОС.

## Ключевые библиотеки

- **cantools** — основной парсер DBC и декодирование физических значений.
- **carbus-lib** (импортируется как `carbus_async`) — заложена для будущей работы с CAN/ISO-TP/UDS транспортом; для парсинга DBC не используется.
- **pyqtgraph** — графики сигналов в реальном времени.
- **PySide6** — GUI-фреймворк.
- **pyserial** — работа с COM-портами.
- **matplotlib** — оставлен для совместимости с существующим графиком `can_graph_tab.py`.
